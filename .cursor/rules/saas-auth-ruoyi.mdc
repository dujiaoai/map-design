---
description: saas-web 认证与会话 — Sprint C/D SaaS 登录·注册·bootstrap·profile，不留 RuoYi
globs:
  - apps/web/app/shared/auth/**
  - apps/web/app/shared/session/**
  - apps/web/app/shared/queries/**
  - apps/web/app/shared/mock/dev-auth.ts
  - apps/web/app/shared/api/**
  - apps/web/app/layouts/**
  - apps/web/app/routes/login.tsx
  - apps/web/app/routes/register.tsx
  - apps/web/app/features/account/**
  - apps/web/app/entities/navigation/**
  - apps/web/app/entities/ruoyi-user/**
  - packages/auth/**
  - packages/ruoyi-api/**
alwaysApply: false
---

# SaaS Auth — 工作台会话（Sprint C/D）

完整说明：[auth-rbac.md](../../docs/architecture/auth-rbac.md)、[backend-integration.md](../../docs/architecture/backend-integration.md)、[services-development-plan.md](../../docs/architecture/services-development-plan.md) **§十**。Skill：`.cursor/skills/saas-auth-ruoyi/`。

## 目标链路（不留 RuoYi）

```
register/login → api-client POST /v1/auth/register 或 /login → auth.setSession
app-layout clientLoader → requireAuthenticated → bootstrapAuthenticatedApp
  → GET /v1/users/me + mock-nav + filterNavByTenant → 失败 clearAppSession → /login
```

## API 选用

| 场景 | 包 | Sprint |
| --- | --- | --- |
| 注册、登录、refresh、logout、users/me | `@repo/api-client` | C |
| 侧栏导航 | mock-nav + registry | C |
| 权限码 / admin API | `@repo/api-client` `/v1/admin/*` | D |
| App 数据层 | `shared/queries/*` + TanStack Query | C/D |

**禁止**新代码调用 RuoYi 登录、`getInfo`、`getMenuRouters`、profile。迁移中遇到旧路径应删除而非双轨。

需配置 `VITE_API_URL` 与 `shared/auth/instance.ts` refresh。

## 新受保护路由

1. 挂 `app-layout` 子路由 — 已有 `clientLoader` 守卫
2. 新 SaaS 数据 → `shared/queries/` + `api-client`
3. 401/403 → `clearAppSession()`

## Mock

`dev-auth.ts` / `MOCK_ACCESS_TOKEN`：本地与 Playwright 无后端时用。

## 禁止

- bootstrap 依赖 RuoYi userInfo / menuRouters
- widget 层 `createRuoYiClient` 或裸 `/YunYanApi`
- 登录/注册/profile 与 RuoYi 并行
- Sprint C/D 实现地图/机库等业务 API（→ Sprint E）
