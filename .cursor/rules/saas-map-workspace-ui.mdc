---
description: saas-web 侧栏菜单与地图工作台 UI 载体（Dock / 浮层 / L4 条带 / Vaul Drawer）选型与文件映射
globs:
  - apps/web/app/widgets/app-sidebar/**
  - apps/web/app/widgets/map-*/**
  - apps/web/app/widgets/dock-panel/**
  - apps/web/app/features/map-workspace/**
  - apps/web/app/features/map-quick-toolbar/**
  - apps/web/app/features/workspace-surface-drag/**
  - apps/web/app/entities/navigation/**
  - apps/web/app/routes/home.tsx
alwaysApply: false
---

# 地图工作台 UI 载体（saas-web）

完整说明见 [map-workspace-ui.md](../../docs/architecture/map-workspace-ui.md)。项目 Skill：`.cursor/skills/map-workspace-ui/`。

## 快速规则

**侧栏分组子菜单** = Collapsible 手风琴（非弹框）。

**侧栏段级折叠**（如「运营」等段）：段标题 + Chevron；持久化 `nav-section-{id}`。高频 **`map-tool` 不在侧栏列表**（方案 C：`MapQuickToolbar` + 顶栏搜索）。catalog：`features/map-quick-toolbar/lib/quick-toolbar-catalog.ts`。UI：`packages/ui/src/components/nav-main.tsx`。

**画布表面拖拽**：浮层与快捷工具条共用 `features/workspace-surface-drag`（@dnd-kit + snap）。勿在 widget 内重复 clamp/snap 逻辑。

**菜单 `kind` → UI：**

| kind | UI |
| --- | --- |
| `map-tool` + `presentation: movable-panel \| anchor` + `category: mode` | `MockMapToolHost` + `activeMapTool`，互斥 |
| `map-tool` + `category: panel` | `MockMapToolHost` + `activePanelTools`，并行、仅手动关闭 |
| `map-tool` + `presentation: drawer` | `MapToolDrawerPanel` 右侧条带（**不是** Vaul） |
| `map-dock-module` | `MapDockPanel`（`MapContextPanel` 内 embedded） |
| `map-module` | `MapBusinessDock`（`MapContextPanel` 内 embedded） |
| `route` / `external` | 路由跳转 / 新窗口，无弹层 |

**页脚**：通知、账号 → Vaul `Drawer`（有遮罩）；账号入口先经 `DropdownMenu`。

**侧栏模块**：`map-module` / `map-dock-module` 全局互斥，左栏 `MapContextPanel` 单层外壳；切换菜单项关闭上一模块。

## 禁止

- 地图工具 L4 条带改用 Vaul Drawer
- 机库/业务模块做成模态 Dialog 挡地图
- 在 `packages/ui` 的 `AppSidebar` 里写业务 store 逻辑（保持在 `widgets/app-sidebar` + `features/map-workspace`）

## 新增菜单

1. `mock-nav-items.tsx` 设 `kind` + id
2. `map-tool` 配 `mockToolMeta.presentation` 与 `coordinatorGroup`
3. 需要深链则改 `workspace-url.ts`
4. 若上快捷工具条：加入 `quick-toolbar-catalog.ts`（1–8 项）
