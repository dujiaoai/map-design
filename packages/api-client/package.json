{
  "name": "@repo/api-client",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "SaaS REST API 客户端（fetch + Bearer + 401 刷新）",
  "exports": {
    ".": "./index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "catalog:",
    "vitest": "^3.0.5"
  }
}
